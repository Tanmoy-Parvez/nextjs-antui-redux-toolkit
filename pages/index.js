import { DatePicker, Button } from 'antd';
const HomePage = () => {
  return (
    <div>
      <DatePicker />
      <Button type="primary">Primary</Button>
    </div>
  );
};

export default HomePage;